import React from "react";
import Footer from "../components/Footer";

const FooterContainer = ({ label, link }) => {
  return <Footer label={label} link={link} />;
};
export default FooterContainer;
